management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: always

---

# internal properties
omdbapi:
  api:
    key: ba73f1b0
    url: http://www.omdbapi.com

---

spring:
  config:
    activate:
      on-profile: test
  datasource:
    url: jdbc:h2:mem:DATABASE
    driver-class-name: 'org.h2.Driver'
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    baseline-on-migrate: true
  #    enabled: false
---

spring:
  config:
    activate:
      on-profile: local
  h2:
    console:
      enabled: true
      settings:
        web-allow-others: true
  datasource:
    url: jdbc:h2:file:./app_db;DB_CLOSE_ON_EXIT=FALSE;AUTO_SERVER=TRUE;INIT=CREATE SCHEMA IF NOT EXISTS JOWL
    username: sa
    password:
    driver-class-name: 'org.h2.Driver'
  sql:
    init:
      continue-on-error: true
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    baseline-on-migrate: true
    locations: 'classpath:db/migration,test_data'

---

spring:
  config:
    activate:
      on-profile: dev
  h2:
    console:
      enabled: true
      settings:
        web-allow-others: true
  datasource:
    url: jdbc:mysql://mysql-service:3306/plex_app_dev
    username: root # change to match secret
    password: password # change to match secret
  sql:
    init:
      continue-on-error: true

---

spring:
  config:
    activate:
      on-profile: prod
  h2:
    console:
      enabled: true
      settings:
        web-allow-others: true
  datasource:
    url: jdbc:mysql://mysql-service:3306/plex_app_dev
    username: root # change to match secret
    password: password # change to match secret
  sql:
    init:
      continue-on-error: true